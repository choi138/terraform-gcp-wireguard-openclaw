name: terraform-ci

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: 1.5.0

      - name: Terraform fmt (root)
        run: terraform fmt -check

      - name: Terraform init (root)
        run: terraform init -backend=false

      - name: Terraform validate (root)
        run: terraform validate

      - name: Terraform fmt (root examples/basic)
        working-directory: examples/basic
        run: terraform fmt -check

      - name: Terraform init (root examples/basic)
        working-directory: examples/basic
        run: terraform init -backend=false

      - name: Terraform validate (root examples/basic)
        working-directory: examples/basic
        run: terraform validate

      - name: Terraform fmt (infra)
        working-directory: infra
        run: terraform fmt -check

      - name: Terraform init (infra)
        working-directory: infra
        run: terraform init -backend=false

      - name: Terraform validate (infra)
        working-directory: infra
        run: terraform validate

      - name: Terraform fmt (examples/basic)
        working-directory: infra/examples/basic
        run: terraform fmt -check

      - name: Terraform init (examples/basic)
        working-directory: infra/examples/basic
        run: terraform init -backend=false

      - name: Terraform validate (examples/basic)
        working-directory: infra/examples/basic
        run: terraform validate

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup TFLint
        uses: terraform-linters/setup-tflint@v6

      - name: TFLint init (shared config)
        run: tflint --init --config ./infra/.tflint.hcl

      - name: TFLint (root)
        run: tflint --config ./infra/.tflint.hcl

      - name: TFLint (infra)
        working-directory: infra
        run: tflint
